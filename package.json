{
  "name": "@hawtio/online-root",
  "version": "0.0.0",
  "private": true,
  "description": "Containerized version of hawtio for kubernetes",
  "author": "Hawtio developer team",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/hawtio/hawtio-online.git"
  },
  "workspaces": [
    "packages/*",
    "docker"
  ],
  "scripts": {
    "start:k8": "yarn build:oAuthapi && yarn build:k8api && yarn start:k8app",
    "start:k8app": "yarn workspace @hawtio/online-kubernetes-api-app start",
    "build:k8": "yarn build:k8api && yarn build:k8app",
    "build:k8app": "yarn workspace @hawtio/online-kubernetes-api-app build",
    "build:k8api": "yarn workspace @hawtio/online-kubernetes-api build",
    "build:k8api:watch": "yarn workspace @hawtio/online-kubernetes-api build:watch",
    "test:k8api": "yarn workspace @hawtio/online-kubernetes-api test",
    "test:k8api:watch": "yarn workspace @hawtio/online-kubernetes-api test:watch",
    "start:oAuth": "yarn build:oAuthapi && yarn start:oAuthapp",
    "start:oAuthapp": "yarn workspace @hawtio/online-oauth-app start",
    "build:oAuth": "yarn build:oAuthapi && yarn build:oAuthapp",
    "build:oAuthapp": "yarn workspace @hawtio/online-oauth-app build",
    "build:oAuthapi": "yarn workspace @hawtio/online-oauth build",
    "build:oAuthapi:watch": "yarn workspace @hawtio/online-oauth build:watch",
    "test:oAuthapi": "yarn workspace @hawtio/online-oauth test",
    "test:oAuthapi:watch": "yarn workspace @hawtio/online-oauth test:watch",
    "lint": "yarn workspaces foreach -v -i -pt --exclude @hawtio/online-root eslint src/",
    "lint:fix": "yarn workspaces foreach -v -i -pt --exclude @hawtio/online-root eslint --fix src/",
    "format:check": "yarn workspaces foreach -v -i -pt --exclude @hawtio/online-root yarn prettier --check .",
    "format:fix": "yarn workspaces foreach -v -i -pt --exclude @hawtio/online-root yarn prettier --write .",
    "image": "docker build -t docker.io/${ORG:-hawtio}/${PROJECT:-online}:${TAG:-latest} .",
    "license": "license-reporter merge --mpn='hawtio-online' --mlx='./packages/online/licenses/licenses.xml,./packages/integration/licenses/licenses.xml' --mo=licenses.xml -o=docker/licenses",
    "gen:proxying": "./scripts/generate-proxying.sh",
    "gen:serving": "./scripts/generate-serving.sh",
    "test:docker": "jasmine docker/*.spec.js",
    "test:nginx": "cd docker/ && HAWTIO_ONLINE_RBAC_ACL= njs test.js",
    "deploy:k8s:namespace": "kubectl apply --kustomize deploy/k8s/namespace/",
    "deploy:k8s:cluster": "kubectl apply --kustomize deploy/k8s/cluster/",
    "deploy:openshift:namespace": "oc apply --kustomize deploy/openshift/namespace/",
    "deploy:openshift:cluster": "oc apply --kustomize deploy/openshift/cluster/ && ./deploy/openshift/cluster/oauthclient.sh $ROUTE_HOSTNAME",
    "kustomize:image": "cd deploy/base && kustomize edit set image hawtio/online=docker.io/${ORG:-hawtio}/${PROJECT:-online}:${TAG:-latest}"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "concurrently": "^8.2.0",
    "cz-conventional-changelog": "3.3.0",
    "eslint": "^8.44.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-config-react-app": "^7.0.1",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-react-hooks": "^4.6.0",
    "prettier": "3.0.0"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "packageManager": "yarn@3.6.1",
  "engines": {
    "yarn": ">=3.6.0",
    "node": ">=16"
  }
}
